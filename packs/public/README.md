# Public Seed Packs

JSON files in this directory are safe to publish and are loaded on first run.

Keep personal character catalogs, LoRA metadata, prompt presets, and production
rules in the ignored private pack directory. Private packs are ignored by Git
and Docker export tooling.
